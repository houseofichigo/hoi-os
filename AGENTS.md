# HOI OS product development

This is the distributable product source. Keep real client data, credentials, private workspaces, exports and execution records outside this repository. Use fictional fixtures only.

Run npm run check after behavioral changes. Canonical runtime skills live in skills/; regenerate .agents and .claude mirrors with npm run skills:sync. Preserve workspace compatibility and backup/restore guarantees. Do not claim Windows/host integration or semantic quality from macOS synthetic tests.

The private workspace is selected explicitly with --workspace. Never infer it from this product checkout. Brand changes follow the company brand rules; the map is optional and may not become the only interface.
